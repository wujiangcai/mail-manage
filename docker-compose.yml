services:
  mail-code-helper:
    build: .
    restart: unless-stopped
    env_file:
      - .env
    ports:
      - "127.0.0.1:17373:17373"
    volumes:
      - mail-code-data:/app/data

volumes:
  mail-code-data:
