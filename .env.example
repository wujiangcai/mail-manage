MAIL_CODE_HOST=0.0.0.0
MAIL_CODE_PORT=17373

# Required for admin panel login.
MAIL_CODE_ADMIN_PASSWORD=change-this-admin-password

# Required for signing browser sessions. Use a long random value.
MAIL_CODE_SESSION_SECRET=change-this-long-random-session-secret

# Required for public deployments. Multiple keys can be separated by commas.
# These keys protect direct server-to-server /api/code and /api/messages calls.
MAIL_CODE_API_KEYS=change-this-long-random-key

# Optional CORS whitelist. Use your HTTPS domain in production.
MAIL_CODE_ALLOWED_ORIGINS=https://mail-code.example.com

# SQLite database path inside the container.
MAIL_CODE_DB_PATH=/app/data/mail-code-helper.sqlite3

# Automatic mailbox polling. Set interval to 0 to disable.
MAIL_CODE_AUTO_POLL_INTERVAL_SECONDS=60
MAIL_CODE_AUTO_POLL_TOP=10

# User page auto-waiting behavior for verification codes.
MAIL_CODE_USER_CODE_POLL_INTERVAL_SECONDS=5
MAIL_CODE_USER_CODE_POLL_TIMEOUT_SECONDS=90

# Admin page realtime refresh interval.
MAIL_CODE_ADMIN_REFRESH_INTERVAL_SECONDS=10
MAIL_CODE_ADMIN_MESSAGE_LIMIT=500
MAIL_CODE_MAX_JSON_BODY_BYTES=5242880

# CPA / Codex credential refresh. These defaults normally do not need changes.
# The CPA base URL and management key are entered in the admin UI per operation.
OPENAI_CODEX_CLIENT_ID=app_EMoamEEZ73f0CkXaXp7hrann
OPENAI_OAUTH_REFRESH_SCOPE=openid profile email
MAIL_CODE_CPA_PROBE_USER_AGENT=codex_cli_rs/0.76.0 (Windows; x86_64) mail-code-helper

# Optional webhook push when a newly seen message contains a verification code.
# Multiple webhook URLs can be separated by commas.
MAIL_CODE_WEBHOOK_URLS=
MAIL_CODE_WEBHOOK_SECRET=
MAIL_CODE_WEBHOOK_TIMEOUT_SECONDS=8
